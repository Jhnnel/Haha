import React, { useState } from 'react';
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View, Button } from 'react-native';
import { Ionicons } from '@expo/vector-icons';

const HomeScreen = () => {
  const [isFlashlightOn, setIsFlashlightOn] = useState(false);

  const handleFlashlightPress = () => {
    console.log('Flashlight button pressed');
    // Toggle the flashlight state
    setIsFlashlightOn(prevState => !prevState);
  };

  const handleCounterPress = () => {
    console.log('Counter button pressed');
    // Add your desired functionality for the counter button here
  };

  return (
    <View style={styles.container}>
      {/* Add your buttons and flashlight icon here */}
      <View style={styles.buttonContainer}>
        <Button title="Flashlight" onPress={handleFlashlightPress} />
        <Button title="Counter" onPress={handleCounterPress} />
      </View>

      {/* Flashlight icon */}
      {isFlashlightOn && <Ionicons name="flashlight" size={40} color="blue" />}

      {/* Existing content */}
      <Text>Hello, world!</Text>
      <StatusBar style="auto" />
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
  buttonContainer: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    width: '100%',
    marginBottom: 20,
  },
});

export default HomeScreen;